# Package

version       = "0.1.0"
author        = "OpenPeeps"
description   = "PayPal post-generation scripts plugin (kapsis shared library)"
license       = "MIT"
srcDir        = "."

# Dependencies

requires "nim >= 2.0.0"
requires "kapsis >= 0.4.0"
requires "pluginkit >= 0.1.0"
